# Bottle Deposits
# Drink containers holding one liter or less have a $0.10 deposit,
# and drink containers holding more than one liter have a $0.25 deposit.

deposit = 0
lo = input("Please tell me how many containers you are holding less than one liter (0 or more)? ")
go = input("and how many containers you are holding more than one liter (0 or more)? ")

if int(lo) > 0:
    lone = float(lo) * 0.10

if int(go) > 0:
    gone = float(go) * 0.25

deposit = lone + gone


print("Your deposit amount is $%.2f" %deposit)
